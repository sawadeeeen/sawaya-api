version: '3'

services:
  api:
    container_name: api
    build: ./api
    ports:
      - "8080:8080"
        #volumes:
        # - ../../../../../:/go
    environment:
      - "GOPATH=/go"
    tty: true
